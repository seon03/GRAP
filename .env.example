# API Keys — copy this file to .env and fill in your values
# Never commit .env to git

OPENROUTER_API_KEY=your_openrouter_api_key_here

# Port of the local vllm server (only needed for LLaMA response generation)
VLLM_PORT=8000
